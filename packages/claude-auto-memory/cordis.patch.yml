# Bundle patch for @cnzgray/dsh-claude-auto-memory: mounts the plugin's own loader entry.
# Applied as a profile layer when this package is installed via
# `dsh plugin --profile <name> add @cnzgray/dsh-claude-auto-memory`.
- insert:
    - id: claude-auto-memory
      name: '@cnzgray/dsh-claude-auto-memory'
