# Bundle patch for @cnzgray/dsh-claude-marketplace-bridge: mounts the plugin's own loader entry.
# Applied as a profile layer when this package is installed via
# `dsh plugin --profile <name> add @cnzgray/dsh-claude-marketplace-bridge`.
- insert:
    - id: claude-marketplace-bridge
      name: '@cnzgray/dsh-claude-marketplace-bridge'
